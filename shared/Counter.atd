(* type id = string wrap <ocaml module="Transform.Id"> *)
type id = string wrap <ocaml module="Transform.Id" wrap="Transform.Id.wrap" unwrap="Transform.Id.to_string">

type t = {
  ?id : id option;
  name : string;
  value : int;
}
type counters = {
  counters: t list;
}
